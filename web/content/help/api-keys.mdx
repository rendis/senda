---
title: API Keys
description: How workspace API keys are created, shown, and used.
---

# API Keys

API keys are the way external services talk to Senda's data-plane APIs.

They are **workspace-scoped**, which means a key belongs to one workspace and should be used only for that workspace's API traffic.

## What happens when you create one

When you generate a new key:

1. Senda creates the key on the server
2. the full secret is shown **once**
3. the list view keeps only a short hint

That one-time reveal is intentional.
If you close the dialog without copying it, you should create a new key.

## How to use it

Use the generated value as a bearer token when calling the API:

```bash
Authorization: Bearer <api-key>
```

The key is for programmatic access, not for logging into the UI.

## APIs available with an API key

Workspace API keys are for the data plane only.

<ApiEndpointReference />

These requests are scoped automatically to the workspace that owns the key.
They do not grant access to management endpoints.

## Sending one message vs batch sending

Use `POST /api/v1/send` when one request shares the same runtime context:

- same template ref
- same variables
- same injector resolution
- one or many recipients in `to` (up to 50)

Use `POST /api/v1/send/batch` when each logical message needs its **own** context:

- same template ref for the whole request
- one primary recipient per item
- per-item `variables`
- per-item `locale`
- per-item `external_id`
- per-item injector/init resolution

That means batch send is the correct endpoint for bulk operations where every message renders differently even though the template is the same.

## Batch limits

The batch endpoint has a clear cap:

- default: **100 items per request**
- configurable through `send.batch_max_items`
- env override: `SENDA_SEND_BATCH_MAX_ITEMS`

If the payload goes over the configured limit, Senda rejects it before processing.

## What you will see in the table

The list view shows:

- name
- hint
- created date
- last used date
- status

The hint is there so you can identify the key later without exposing the secret.

## Where to find the API docs

If you are building against the API, open the workspace **API Docs** page from the sidebar.

That page contains the interactive OpenAPI reference and is the best place to copy request examples.

## Good practice

- give each key a descriptive name
- create one key per app or integration
- revoke keys you no longer use

## Related docs

- [Send flow](send-flow)
- [FAQ](faq)
