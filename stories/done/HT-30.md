---
id: HT-30
title: "Onboarding Wizard (3 pasos)"
epic: E7 — Frontend
status: done
priority: high
estimate: 3
dependencies: [HT-25, HT-29]
spec_sections: [§20]
track: E
---

# HT-30: Onboarding Wizard (3 pasos)

## Objetivo
Implementar el flujo de onboarding para primer uso: Welcome → Crear Tenant → Crear Workspace. Solo se muestra si `onboarding.completed = false`.

## Entregables

1. **Welcome (paso 1)**
   - Leer via Pencil MCP el frame "Screen/Onboarding Welcome" (Layer 7)
   - Stepper visual (3 pasos), descripción, botón "Comenzar"

2. **Crear Tenant (paso 2)**
   - Leer via Pencil MCP el frame "Screen/Onboarding Tenant" (Layer 8)
   - Form: nombre + código (auto-slug desde nombre)
   - Validación con Zod: `[a-z][a-z0-9-]*`, 2-50 chars, sin `--`, sin guion final

3. **Crear Workspace (paso 3)**
   - Leer via Pencil MCP el frame "Screen/Onboarding Workspace" (Layer 9)
   - Form: nombre + código (mismas reglas de validación)
   - Submit → POST `/api/v1/manage/onboarding`

4. **Lógica de flujo**
   - Check `GET /api/v1/manage/onboarding/status` al cargar la app
   - Si `needed: true` → redirect a /onboarding
   - Bloquear navegación hasta completar
   - Al completar → redirect a dashboard

## Reutilización

**Componentes que reutiliza de HT-28:**
- `FormDialog` — formularios de Tenant y Workspace (pasos 2 y 3)
- `StatusBadge` — indicador visual de paso completado en stepper

**Componentes nuevos a extraer:**
- `StepWizard` — stepper visual reutilizable para multi-step forms
- `OnboardingProgress` — indicador de progreso visual

## Metodología

Feature-first: esta HT entrega el flujo onboarding end-to-end. Reutiliza `FormDialog` de HT-28 en los pasos 2 y 3 en lugar de crear forms individuales. Extrae `StepWizard` y `OnboardingProgress` como componentes de propósito general que podrían usarse en otros flujos wizard en el futuro.

## Criterios de Aceptación

- [ ] Wizard de 3 pasos funciona end-to-end
- [ ] Validación de códigos en frontend (Zod) matchea backend
- [ ] POST a onboarding endpoint crea tenant + workspace + asigna superadmin
- [ ] Diseño matchea frames de Pencil (via MCP)
- [ ] Redirect a dashboard post-onboarding

## Definition of Done (DoD)

Cada feature se considera terminada SOLO cuando cumple estos 3 gates:

### Gate 1: Validación Visual (Pixel-Perfect)
1. Levantar la app en dev (`pnpm --dir web dev`)
2. Navegar a la pantalla implementada
3. Tomar screenshot via herramienta (o manualmente)
4. Leer el frame correspondiente del diseño via Pencil MCP
5. Comparar screenshot vs diseño de Pencil lado a lado
6. Verificar:
   - [ ] Layout y espaciado coinciden
   - [ ] Colores y tipografía correctos
   - [ ] Componentes del Design System usados (no custom)
   - [ ] Responsive: verificar en 1440px, 1024px, 768px
   - [ ] Empty states coinciden con diseño
   - [ ] Dark/light mode si aplica
7. Si hay drift visual → corregir en código, NO en Pencil

### Gate 2: Integración con Backend
1. Conectar contra backend real (docker-compose up)
2. Verificar cada endpoint que la feature consume:
   - [ ] GET lista carga datos reales
   - [ ] POST/PUT crea/edita correctamente
   - [ ] DELETE (soft) funciona con confirmación
   - [ ] Errores del backend se muestran correctamente (toast + mensaje)
   - [ ] Loading states mientras espera respuesta
   - [ ] Paginación cursor-based funciona
   - [ ] Filtros y búsqueda funcionan
3. Verificar auth:
   - [ ] Sin token → redirect a login
   - [ ] Token expirado → refresh o redirect
   - [ ] Rol insuficiente → mensaje de error, no crash

### Gate 3: Funcionalidad Completa
1. Todos los criterios de aceptación de la HT cumplidos
2. Componentes reutilizados del Design System (no duplicados)
3. Nuevos componentes extraídos a `components/shared/` si aplican
4. Zero errores en console del browser
5. Zero warnings de TypeScript
6. `pnpm --dir web lint` pasa
7. `pnpm --dir web typecheck` pasa sin errores

## Notas de Implementación

_Pendiente._

## Log de Progreso

_Pendiente._
