---
id: HT-15
title: "SendService — Orchestration Core"
epic: E4 — Send Flow
status: backlog
priority: 15
estimate: 5 days
dependencies: ["HT-10", "HT-11", "HT-12", "HT-13", "HT-14"]
spec_sections: ["§13"]
track: D
---

# HT-15: SendService — Orchestration Core

**Épica:** E4 — Send Flow
**Spec:** §13 (SendService — Flujo Principal)

## Objetivo

Implementar el servicio principal de envío que orquesta resolución, compilación, y encolado.

## Entregables

- `internal/service/send.go` — `SendService`
  - `Send(ctx, request) → (trackingID, error)`
  - Flujo completo: parse address → resolve chain → resolve template → merge injectors → render variables → resolve adapter → validate domain → check suppression → compile MJML → create email record → enqueue job → return tracking ID
- Request DTO validation

## Criterios de Aceptación

- [ ] Happy path: send request → email record created → job enqueued → tracking ID returned
- [ ] Suppressed recipient → 422 con motivo
- [ ] No adapter configured → 422
- [ ] Unverified domain → 422
- [ ] Template not found → 404
- [ ] Invalid variables (schema mismatch) → 400
- [ ] Snapshots guardados (variables, injectors) para auditoría
- [ ] Tests unitarios con todos los resolvers mockeados
- [ ] Integration test con stack real

## Notas de Implementación

## Log de Progreso
